{% extends 'base.html.twig' %}
{% block title %}
	{{parent()}}Liste des parcelles
{% endblock %}
{% block body %}
	<div class="container-fluid">
		<h1 class="text-center text-primary mt-4 pt-4 display-1 fw-bold">
			Liste des parcelles</h1>
		<div class="row justify-content-center">
			<div class="col-12 col-md-8 bg-white p-4 m-0 text-primary text-center">
				<div class="table-responsive">
					{{ form_start(form) }}
					<table class="table table-hover">
						<thead>
							<tr class="fw-bold text-primary">
								<th scope="col">surface</th>
								<th scope="col">nomparcelle</th>
								<th scope="col">coordonnees</th>
								<th scope="col"></th>
								<th scope="col"></th>
								<th scope="col"></th>
							</tr>
						</thead>
						<tbody>
							{% for parcelle in parcelles |sort((a, b) =>a.nomparcelle <=> b.nomparcelle) %}
								<tr class="{{ cycle(['table-primary', 'table-secondary'], loop.index0) }}">
									<td class="textstart">{{ parcelle.surface | capitalize }}</td>
									<td class="textstart">{{ parcelle.nomparcelle | capitalize }}</td>
									<td class="textstart">{{ parcelle.coordonnees | capitalize }}</td>
									<td>
										<a href="{{path("app_modifier-parcelle",{'id':parcelle.id})}}">
											<i class="bi bi-pen-fill text-dark"></i>
											
										</a>
									</td>
									<td>
										<a href="{{path("app_supprimer-parcelle",{'id':parcelle.id})}}">
											<i class="bi bi-trash-fill text-dark"></i>
										</a>
									</td>
									<td>{{ form_widget(form.parcelles1[parcelle.id]) }}</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
					{{ form_widget(form.supprimer) }}
					{{ form_end(form) }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
